# ClawTech「LP自動流入動線」設計草案

**版**: v1.0 (2026-07-13) / **作成**: Fable5(思考骨格設計) / **実装担当**: Sonnet5等
**前提**: 既存のブログ自動生成(cron 毎日8:30)・git push・sitemap自動化・NucBox同期を**壊さない**。すべて加算的な変更で構成する。

---

## 1. 結論サマリー

**「ブログ=SEO特化」と「ブログ=流入動線」は二者択一ではない。デジライズLPの本質は「SEO記事(入口) → 診断ツール/資料(中間変換装置) → セグメント型フォーム(出口)」の分業であり、ClawTechは入口(記事79本自動生成)は既に完成、中間変換装置がゼロなのが唯一かつ最大のボトルネック。**
よって結論: ブログはこのままSEO特化量産を継続し、**投資対象は「DX無料診断ツール」+「記事CTAの診断誘導化」+「フォームのセグメント化」の3点**に集中する。

---

## 2. ギャップ分析

デジライズLPの各要素を「本質(何のための装置か)」に還元し、ClawTech現状と比較する。

| # | デジライズの要素 | 本質(機能) | ClawTech現状 | ギャップ | 優先度 |
|---|---|---|---|---|---|
| 1 | 無料AI組織診断(6問30秒→Lv判定→PDF) | **最強のリード獲得装置**。問い合わせより心理障壁が低い中間CV | **ゼロ**。ただし`simulator.html`(質問→結果→CTA構造)が別事業向けに存在し、骨格は流用可能 | 中間CV装置が存在しない | **高** |
| 2 | ブログ=業務×業界ロングテールSEO記事群 | 検索流入の入口 | **ほぼ完成**(79本稼働、キーワード選定→執筆→公開→sitemap全自動) | 記事末CTAが「無料相談」直行のみで飛距離が長すぎる | **高**(CTA差し替えのみ) |
| 3 | セグメント型問い合わせフォーム(資料請求/相談を分岐、規模・興味を取得) | リードの温度・属性の仕分け | 名前/会社/メール/相談内容select/メッセージのみ(Formspree)。「資料請求のみ」の逃げ道がない | 温度の低いリードを取りこぼす | **高** |
| 4 | 事例「課題→実装→成果(数値)」+個別ページ | 数値による信頼獲得 | works/ 3本(beauty/gym/saas)は業種テンプレで実数値なし | 実測値事例ゼロ。ただし**自社運営の自動化実績は実在する数値** | **中** |
| 5 | ホワイトペーパー(メールでPDF) | メールアドレス獲得(ナーチャリング入口) | ゼロ。ただし原料(79記事)は既にある | 記事群→PDF再編集の仕組みがない | **中** |
| 6 | 「そもそも○○とは」教育セクション | 非専門家ターゲットの離脱防止 | LPに明示セクションなし | 「DXとは/MEOとは」の30秒教育がない | 低 |
| 7 | 講演・著書・メディアの権威付け | 個人ブランドによる信頼 | ゼロ。**UUは顔出ししない方針** | 丸コピー不可。代替: 「AIが運営する会社」自体を実績数値で見せる(§7) | **中** |
| 8 | 導入企業ロゴ・実績数字(500社+等) | 社会的証明 | 「お客様の声」のみ、数値バナーなし | 自社で証明可能な数値(記事本数・自動化率・応答24h)で代替 | 中 |
| 9 | サービス5階層+成果物単位の明確料金 | 検討段階別の受け皿 | 3段階料金プランあり | 概ね充足。診断結果→推奨プランの紐付けがない | 低 |
| 10 | LP内ブログプレビュー3件 | 回遊 | **実装済み** | なし | — |
| 11 | 記事内インフォグラフィック/図解 | 滞在時間・被リンク | テキストのみ | ミニアプリ埋め込みで代替・上回れる(§5) | 中 |

**要約**: 高優先ギャップは (1)診断ツール (2)記事CTAの中間装置化 (3)フォームセグメント化 の3つ。すべて「変換装置」側であり、入口(SEO)側の追加投資は不要。

---

## 3. 動線設計(ファネル)

### 3.1 全体ファネル図

```
【入口】検索流入
  ├─ SEO記事 79本+毎日1本 (blog/*.html)          ← 既存・変更最小
  ├─ ツール型記事 週1本 (tools/*.html + 紹介記事)  ← 新設(§5)
  └─ LP直接流入 / MEO / SNS
        │
        ▼
【中間変換装置】← ★今回の投資対象。ここが現状ゼロ
  ├─ (A) 無料DX成熟度診断  /shindan.html   6問30秒 → Lv1-5判定
  ├─ (B) 業種別ツール群     /tools/…       粗利計算・MEO順位シミュレーター等
  └─ (C) 無料資料DL        /docs/…        既存記事再編集PDF(メール取得)
        │
        │  診断結果Lv・回答・流入元slugをURLパラメータ/hiddenで引き継ぐ
        ▼
【出口】セグメント型フォーム (#contact 拡張)
  ├─ 「資料請求のみ」(低温度) → PDF送付 → メールナーチャリング(sora連携)
  ├─ 「診断結果の詳細レポート希望」(中温度) → 個別レポート送付+提案
  └─ 「無料相談希望」(高温度) → 24h以内返信(既存フロー)
        │
        ▼
【計測】GA4 + kpi-ledger で段階別CVRを週次記録
```

### 3.2 中間装置(A): 無料DX成熟度診断 — ClawTech版「AI組織診断」

デジライズの「AI成熟度Lv1-5」をClawTechのターゲット(中小企業経営者・個人事業主・40代男性)に翻案する。

**設置場所**: `~/clawtech-lp/shindan.html`(単独ページ)+ LPヒーローに第2CTA「無料DX診断(30秒)」+ 各ブログ記事CTAボックスから誘導。

**設問設計(6問・すべてタップ選択・30秒)**:

| # | 設問 | 選択肢の軸 |
|---|---|---|
| 1 | 業種は? | 建設/飲食/美容・サロン/医療・整骨院/士業/小売/その他 ※記事CTAのターゲット業種マップと揃える |
| 2 | 自社ホームページはありますか? | ない/古い(5年以上更新なし)/ある(更新中)/LPも運用中 |
| 3 | Googleマップで自社を検索したことは? | ない/あるが未登録/登録済み放置/クチコミ返信まで運用 |
| 4 | 見積書・請求書・予約管理はどうしてる? | 紙・電話/Excel/一部ツール/ほぼ自動化 |
| 5 | AIを業務で使ったことは? | ない/個人で少し/一部業務で/組織的に活用 |
| 6 | 新規集客の主な経路は? | 紹介・口コミのみ/チラシ・広告/Web半々/Web中心 |

**判定ロジック(クライアントサイドJSで完結、即時表示)**:
- 各回答0〜3点、合計0〜18点 → **DX成熟度 Lv1「紙と電話」〜Lv5「AI活用経営」**の5段階
- Lvごとに「あなたの次の一手」として**ClawTechサービスを1〜2個だけ**推奨(Lv1→HP制作¥49,800〜、Lv2→MEO対策、Lv3→予約システム/業務自動化、Lv4→AI営業代行、Lv5→DXコンサル顧問)
- Q1の業種回答で推奨文言を業種特化に差し替え(auto_blog_process.py内の業種別訴求マップと同一辞書を使う)

**リード取得の2段構え**(デジライズと同じ設計):
1. 結果はその場で**無料表示**(メール不要 — 離脱防止)
2. 「業種別の詳細レポート(改善ロードマップ付きPDF)をメールで受け取る」ボタン → Formspreeへ `診断Lv/回答6つ/流入元slug` をhidden送信 → 後続バッチが個別PDFを生成しメール送付(§4のスキル参照。初期はテンプレPDFのLv別5種で開始し、後で個別化)

**実装形態**: simulator.html と同じ**自己完結単一HTML**(外部依存なし、モバイルファースト、GA4イベント `shindan_start / shindan_complete / shindan_lead` を発火)。LPのデザイントークン(DESIGN.md: --primary #0052CC / --accent #FF6B35 / Noto Sans JP)に準拠し、simulator.htmlの青系スタイルはそのまま流用しない(あれは別事業のトーン)。

### 3.3 中間装置(B): 業種別ミニツール群 → §5で詳述

### 3.4 中間装置(C): 無料資料(ホワイトペーパー)

- 原料は既存79記事。カテゴリ集計(Web制作23/集客22/AI活用16/DX13/SNS運用13/SEO10…)から、記事6〜10本を1テーマに再編集した**PDFガイド**を作る:
  1. 『中小企業のためのAI業務自動化 完全ガイド2026』(AI活用+自動化+DX系記事から)
  2. 『Google集客(MEO×SEO)はじめてガイド』(MEO+SEO+集客系から)
  3. 『ホームページ・LP発注前チェックリスト』(Web制作系から)
- 配布ページ `~/clawtech-lp/docs/` に一覧、DLはメール必須(Formspree)。記事CTAボックスの第2ボタンとして「関連ガイドを無料DL」を出し分ける。
- **重要**: PDF生成は手作業にしない。記事HTML→Markdown抽出→再構成→PDF化を`lead-magnet`スキル(§4)として自動化する。

### 3.5 記事側CTAの改修(飛距離の最適化)

現状 `auto_blog_process.py` L201-206 のCTAは2択(sales/ or #contact)で、いずれも**高温度CV直行**。これを3段CTAに変更する:

```
記事末CTAボックス(新):
  [主ボタン]  30秒でわかる 無料DX診断 → ../shindan.html?src={slug}
  [副ボタン]  カテゴリ対応: AI営業系→../sales/ それ以外→../#contact (既存ロジック維持)
  [テキストリンク] 関連無料ガイドDL → ../docs/{カテゴリ対応PDF}
```

- 新規記事: テンプレ変更のみ(§5参照)
- **既存79記事**: `.cta-box`ブロックをregex一括置換するretrofitスクリプトを1回実行(バックアップ→置換→lp_analyzer検証→git push)。ブログ本文には触れない。

### 3.6 流入元トラッキング

- 診断/フォームへのリンクに必ず `?src={slug}` を付与し、shindan.html→Formspree hidden fieldへ引き継ぐ
- これにより「どの記事がリードを生んだか」が**Formspree通知メールだけで**判別可能になり、blog_trend_keywords.pyのキーワード選定に成果フィードバックをかけられる(§4 funnel-analytics)

---

## 4. 不足スキルの洗い出し

既存スキル体系(`~/.openclaw/skills/<name>/SKILL.md`、frontmatter: name/description/version/allowed-tools 形式。lp-analyzer等が参考実装)に倣い、以下を新設する。

### 4.1 `tool-forge`(新規・最重要)
- **役割**: 自己完結型HTMLミニアプリ(診断・シミュレーター・チェックリスト・計算機)を仕様から生成し、QAして`~/clawtech-lp/tools/`へ配置する。simulator.htmlを骨格テンプレとして抽象化した`tools/_template.html`を管理。
- **トリガー**: 「診断ツール作って」「シミュレーター追加」/ ブログパイプラインからの自動呼び出し(§5)
- **必須工程**: 生成→Playwrightでヘッドレス動作確認(全設問タップ→結果表示→コンソールエラー0件)→GA4イベント埋め込み確認→lp-deploy手順でpush
- **allowed-tools**: Read, Write, Bash, Playwright系

### 4.2 `dx-shindan`(新規)
- **役割**: shindan.html本体の運用。設問・配点・Lv別推奨サービス辞書のメンテ、Lv別PDFレポート5種の生成・更新、回答データの集計(どのLvのリードが多いか→LP訴求へ反映)
- **トリガー**: 「診断の設問見直して」「診断リード集計して」/ 月次自動レビュー
- **依存**: tool-forge(初回生成)、kpi-ledger(リード数記録)

### 4.3 `lead-magnet`(新規)
- **役割**: 既存ブログ記事群からテーマ別PDFガイドを自動編纂(記事抽出→重複削除→章立て再構成→表紙/CTAページ付きPDF化)。`docs/`配布ページの更新、記事CTAとの紐付けマップ管理。
- **トリガー**: 「ホワイトペーパー作って」/ 四半期ごと(記事が30本増えるごと)に改訂版を自動生成
- **注意**: 記事は毎日増えるのでPDFは「生成物」扱い。ソースオブトゥルースはblog_articles.db

### 4.4 `case-forge`(新規)
- **役割**: 「課題→実装→成果(数値)」形式の事例ページ生成(§6の方針に従う)。ネタ元は (a)実案件 (b)自社自動化実績(ログ・DBから実数値を自動収集: 記事公開本数、営業メール送信数、稼働スクリプト数等) (c)works/テンプレのデモ明記化
- **トリガー**: 「事例ページ作って」「今月の自社実績を事例化して」/ kpi-ledgerに新規成果が記録された時
- **鉄則**: auto_blog_process.pyのE-E-A-Tルールと同じ——**実在しない数値は絶対に創作しない**。自社ログから取れる実測値のみ使用

### 4.5 `funnel-analytics`(新規・既存analytics_report.py/kpi-ledgerの拡張)
- **役割**: 週次で「記事別流入(GA4)→診断開始/完了(GA4イベント)→リード(Formspree/受信メール)→相談」の段階別CVRを算出し、Discord報告+`src`別成果をblog_trend_keywords.pyのキーワード優先度に反映(成果の出たカテゴリのキーワードを増産)
- **トリガー**: 毎週月曜(blog_trend_keywords.py実行の直前が理想 — 成果データを踏まえてキーワード選定)
- **依存**: ga4_report.py(既存)、kpi-ledger(既存)

### 4.6 `lead-nurture`(新規・フェーズ3)
- **役割**: 資料請求/診断レポート希望の低〜中温度リードに対する自動フォローメール(3通ステップ: PDF送付→3日後に関連記事→7日後に無料相談案内)。既存sora_mailwatch.py/gmail系スクリプトと連携。メール送信上限(10件/日)ルールを厳守。
- **トリガー**: Formspree通知メールの受信検知(sora経由)

### 既存スキルの改修(新設不要)
- **lp-deploy**: tools/ docs/ shindan.html を対象パスに追加するだけ
- **lp-analyzer**: 診断ページ用チェック項目(GA4イベント有無、フォームhidden欠落)を追加
- **kpi-ledger**: リード種別(診断/資料/相談)のカラム追加

---

## 5. ブログ生成パイプラインへのアプリ組み込み設計

### 5.1 出し分けの基本方針

**テキスト記事は毎日(現状維持)、ツール型記事は週1本を上限**とする。理由: (a)ツールはQAコストが高い (b)SEO的にはテキスト量産の方が効率的 (c)ツールは1本で長期間リードを生む資産型なので量は不要。

### 5.2 どのキーワードをツール化するか(判定の入れ場所)

**判定は`blog_trend_keywords.py`(毎週月曜のキーワード選定時)に組み込む**。生成時(auto_blog_process.py)ではなく選定時に決めるのが要点 — キーワードの検索意図が「計算・診断・比較」型かどうかで機械的に判定できるため。

- LLMプロンプトに追加: 各キーワードに `article_type: "text" | "tool"` と、toolの場合 `tool_concept`(1行仕様)を付けさせる
- tool判定基準(プロンプトに明記): 「費用/料金/相場」「何日かかる/期間」「診断/チェック」「比較」「シミュレーション」等、**数値入力→数値出力またはチェックリスト化が自然な検索意図**のみ
- 週にtool判定が2件以上出ても、DBに入れるのは1件まで(残りはtextに降格)

### 5.3 DBスキーマ変更(後方互換)

```sql
ALTER TABLE articles ADD COLUMN article_type TEXT DEFAULT 'text';
ALTER TABLE articles ADD COLUMN tool_concept TEXT;
```
既存行・既存コードはDEFAULT 'text'で無影響。

### 5.4 auto_blog_generator.sh の分岐(既存フロー無変更の加算)

```
ROW取得時にarticle_typeも取得
  ├─ article_type = 'text'  → 現行どおり auto_blog_process.py (一切変更なし)
  └─ article_type = 'tool'  → 新規 auto_tool_process.py を呼ぶ
                               (失敗時はstatus='error'にして現行のエラーフローに乗せる。
                                textへのフォールバック再生成も可)
git push / sitemap再生成は共通末尾処理のまま
```

### 5.5 auto_tool_process.py(新規)の生成フロー

1. **ツール本体生成**: `tools/_template.html`(simulator.htmlから抽出した骨格: ヒーロー→入力フォーム→結果カード→ゲージ→CTA→シェアボタン、ClawTechデザイントークン適用)にLLMで「設問/入力項目・計算ロジックJS・結果パターン文言」を流し込む → `~/clawtech-lp/tools/{slug}.html`
2. **QAゲート(必須)**: Node/Playwrightヘッドレスで (a)全入力パターン1周 (b)結果表示確認 (c)console error 0件 (d)auto_blog_process.pyと同様のfull-documentマーカー検証。**失敗したらpublishせずerror** — 壊れたツールを公開しないことが最優先
3. **紹介記事生成**: 通常記事と同構成のSEO記事を生成し、記事中盤にツールへの誘導ブロック(iframe埋め込みではなく**ボタンリンク**推奨 — モバイルUXとCLS対策)を挿入 → `blog/{slug}.html`
4. **登録**: blog/index.htmlカード追加(既存ロジック流用、バッジを「🔧ツール」に)、tools/一覧ページ更新、CTAは診断/相談への2段構え

### 5.6 ツールのCV設計(ここを忘れると単なる無料おもちゃになる)

- すべてのツールの結果画面に「この数字を改善する方法を無料相談」+「より詳しいDX診断(shindan.html)」の2CTAを固定装備(テンプレ側で強制)
- GA4イベント `tool_start / tool_complete / tool_cta_click` をテンプレ側で標準実装

### 5.7 ツール企画の初期タネ(業種×検索意図から)

| ツール | 対象キーワード群 | 対応サービス |
|---|---|---|
| ホームページ制作費用シミュレーター | 「ホームページ制作 費用 相場」 | LP制作 |
| MEO集客インパクト試算(来店数×客単価) | 「MEO対策 効果」 | MEO |
| 事務作業の自動化削減時間診断 | 「業務自動化 事例」 | 業務自動化 |
| AI営業代行 vs 営業採用 コスト比較 | 「営業代行 費用」 | AI営業代行 |
| 補助金対象チェッカー(IT導入補助金) | 「IT導入補助金 対象」 | DXコンサル |

---

## 6. 事例ページ強化案

### 6.1 三層構成(実データが乏しい現実への対処)

実案件が少ない前提で、**嘘をつかずに数値の説得力を出す**ために事例を3種類に分類し、それぞれ正直にラベリングする:

**層1: 自社実証事例(最強・今すぐ作れる)** — 「ClawTech自身がClawTechの最初の顧客」という打ち出し。デジライズの「代表自身が実践者」の翻案であり、顔出しなしで権威付けが成立する。ログ・DBから取れる**実測値**で構成:
- 事例CT-001『ブログ運用の完全自動化』: 課題=1人運営で記事が書けない → 実装=キーワード選定〜公開まで全自動パイプライン → 成果=**79本公開・人的工数ほぼゼロ・毎日1本ペース継続中**(blog_articles.dbから自動集計)
- 事例CT-002『LPのAI改善サイクル』: 課題=CV率低迷 → 実装=lp_analyzer.pyで自動採点・改善 → 成果=**CTA 102個→8個に整理、スコア72→(最新値)**(MEMORY.md記録の実数)
- 事例CT-003『営業アプローチの自動化』: kai_outreach.pyの送信実績・ray_autofixの自動修復件数など、`~/.openclaw/logs/`から実数を収集
- → これがそのまま「業務自動化・AI営業代行サービスの生きたデモ」になる

**層2: 実案件事例** — 受注が発生し次第、case-forgeスキルで「課題→実装→成果」型ページを追加。数値が出るまでは「導入直後の声」として定性で掲載し、3ヶ月後に数値追記するワークフローをスキルに組み込む。

**層3: 既存works/ 3本の正直化** — beauty/gym/saas は削除せず**「制作デモ(このクオリティを¥49,800〜で)」と明記**して「デザインサンプル」セクションへ移動。偽装事例と受け取られるリスクを消しつつ制作力の証明として活用。

### 6.2 ページ構造(デジライズ /cases/CC-001 形式の翻案)

- `~/clawtech-lp/cases/ct-001.html` 形式で個別ページ化(SEO資産にもなる)
- 統一フォーマット: 課題(Before) → 実装内容(What/How・使用技術) → 成果(数値+計測方法を注記) → 「同じ課題をお持ちの方へ」CTA(診断+相談)
- LP works セクションは「自社実証事例(層1)を先頭」に並べ替え

### 6.3 権威付けセクションの代替案(顔出しなし)

LPに「**数字で見るClawTechの自動化力**」バンドを新設: 「自動公開ブログ記事 79本+」「24時間365日AI稼働」「問い合わせ返信24h以内」「自動化スクリプト稼働数 n本」— すべて自社ログの実数。**数値はfunnel-analyticsが月次で自動更新**する(手で書いた数字は腐るため)。

---

## 7. 問い合わせフォームのセグメント化案

現行Formspreeフォーム(index.html #contact)への加算的変更。フォーム送信先・既存フィールドは変えない。

### 7.1 追加フィールド

| フィールド | 種別 | 必須 | 選択肢 |
|---|---|---|---|
| **ご希望の対応**(最重要) | radio | 必須 | ①まず資料だけ欲しい ②無料DX診断の詳細レポート希望 ③無料相談したい(オンラインOK) |
| 会社規模 | select | 任意 | 1人/2〜5人/6〜20人/21〜50人/51人〜 |
| ご予算感 | select | 任意 | 未定/〜5万/〜15万/〜50万/50万〜/月額顧問希望 |
| 検討時期 | select | 任意 | すぐに/1ヶ月以内/3ヶ月以内/情報収集段階 |
| (hidden) src | hidden | — | 流入元記事slug/診断Lv/ツール名(URLパラメータから自動セット) |

- 既存の「ご相談内容」selectはそのまま維持
- `_subject` を「ご希望の対応」に応じてJSで動的変更(例:「【資料請求】」「【診断レポート希望】」「【無料相談】」)→ 受信メールの件名だけで温度が判別でき、lead-nurture(§4.6)の自動振り分けが可能になる

### 7.2 UX原則

- 必須項目は現行3つ(名前・メール・希望対応)から増やさない。セグメント項目はすべて任意 — デジライズも必須は最小限
- 「①資料だけ」を選んだ場合はメッセージ欄を非表示にして送信ハードルを下げる

---

## 8. 実装ロードマップ

### フェーズ1: 高インパクト即効施策(1週間以内 / 既存パイプライン無変更)

| # | タスク | 成果物 | 依存 |
|---|---|---|---|
| 1-1 | DX成熟度診断 v1 作成(simulator.html骨格流用、ClawTechトーン適用、判定ロジック静的JS、GA4イベント) | `~/clawtech-lp/shindan.html` | なし |
| 1-2 | LPヒーローをデュアルCTA化(「無料相談」+「無料DX診断30秒」) | index.html改修 | 1-1 |
| 1-3 | フォームセグメント化(§7) | index.html #contact改修 | なし |
| 1-4 | 既存79記事のCTAボックス一括retrofit(診断リンク+src付与。regex置換→lp_analyzer検証→push) | retrofitスクリプト(1回実行) | 1-1 |
| 1-5 | 新規記事テンプレのCTA差し替え(auto_blog_process.py L253-278のcta-box部のみ変更) | auto_blog_process.py 小改修 | 1-1 |
| 1-6 | works/3本に「制作デモ」明記(§6層3) | works/*.html 小改修 | なし |

**フェーズ1完了時点で「記事→診断→セグメント済みリード」の動線が全記事で開通する。** デプロイは既存lp-deploy手順(git push→NucBox pull→sitemap)を踏襲。

### フェーズ2: 変換装置の増強(2〜4週間 / パイプライン拡張)

| # | タスク | 成果物 |
|---|---|---|
| 2-1 | 自社実証事例3本(CT-001〜003)を実ログから作成、LP worksセクション再編 | `cases/ct-00x.html` |
| 2-2 | 「数字で見るClawTech」実績バンドをLPに追加 | index.html |
| 2-3 | PDFガイド第1弾(AI業務自動化ガイド)+ docs/配布ページ+記事CTA第3ボタン | `docs/` 一式 |
| 2-4 | ブログDBに`article_type`/`tool_concept`列追加、blog_trend_keywords.pyにtool判定追加 | DB migration + py改修 |
| 2-5 | tools/_template.html 整備(simulator.html骨格の抽象化+CV/GA4標準装備) | テンプレ |
| 2-6 | auto_tool_process.py 新規作成+generator.sh分岐+PlaywrightQAゲート | 週1ツール記事の自動化 |
| 2-7 | 手動で初弾ツール1本(HP制作費用シミュレーター)を先行公開しCV検証 | `tools/` 1本 |

### フェーズ3: スキル化・計測ループ(1〜2ヶ月)

| # | タスク | 成果物 |
|---|---|---|
| 3-1 | 新スキル正式登録: tool-forge / dx-shindan / lead-magnet / case-forge(§4のSKILL.md形式) | `~/.openclaw/skills/` 4件 |
| 3-2 | funnel-analytics: 週次段階別CVRレポート(月曜、blog_trend_keywords.py直前)+成果フィードバック | cron 1本+py |
| 3-3 | lead-nurture: Formspree通知→sora検知→3通ステップメール(上限10件/日厳守) | py+sora連携 |
| 3-4 | 診断の個別PDFレポート化(Lv別テンプレ5種→回答個別化へ段階移行) | dx-shindan拡張 |
| 3-5 | 実案件事例の追加運用フロー(受注→3ヶ月後数値追記)をcase-forgeに組込 | 運用ルール |

### インフラ保全ルール(全フェーズ共通)
- cron変更・新規スクリプト作成はCLAUDE.mdの権限規定に従う(crontab追加はClaude Code自律判断可だがDiscord報告)
- auto_blog_process.py本体のテキスト記事ロジックは**CTAブロック以外触らない**
- 一括retrofit前に`~/clawtech-lp`の該当ファイルをバックアップ、置換後にlp_analyzer.pyで検証してからpush
- 数値を語る箇所は必ずログ/DB由来の実測値(E-E-A-Tルール準拠、創作禁止)

### KPI(月商100万円目標への接続)
- 北極星: 週次リード数(診断完了→レポート請求+資料DL+相談の合計)
- フェーズ1後の測定基準: 記事セッション→診断開始率5%、診断開始→完了率60%、完了→リード率20%を初期仮説とし、funnel-analyticsで実測補正

---

### Critical Files for Implementation

- `/home/uu/clawtech-lp/index.html` (ヒーローCTA・フォーム・実績バンド・works再編の改修対象)
- `/home/uu/.openclaw/scripts/auto_blog_process.py` (記事CTAブロック改修+新テンプレの参照元)
- `/home/uu/.openclaw/scripts/auto_blog_generator.sh` (article_type分岐の追加箇所)
- `/home/uu/clawtech-lp/simulator.html` (診断ツール/tools/_template.htmlの骨格流用元)
- `/home/uu/.openclaw/scripts/blog_trend_keywords.py` (tool判定の組み込み先)
- `/home/uu/.openclaw/skills/lp-analyzer/SKILL.md` (新規スキル4本のフォーマット参照元)
